{
  "name": "db",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "json:server":"json-server --watch ./db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
